<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unicorn.gameshop.order.mapper.OrderMapper">
    <resultMap id="orderMap" type="com.unicorn.gameshop.order.model.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="buyerId" column="buyer_id"/>
        <result property="productId" column="product_id"/>
        <result property="gameId" column="game_id"/>
        <result property="playerUid" column="player_uid"/>
        <result property="quantity" column="quantity"/>
        <result property="amountMinor" column="amount_minor"/>
        <result property="currency" column="currency"/>
        <result property="country" column="country"/>
        <result property="accessTokenHash" column="access_token_hash"/>
        <result property="orderStatus" column="order_status"/>
        <result property="paymentStatus" column="payment_status"/>
        <result property="fulfillmentStatus" column="fulfillment_status"/>
        <result property="riskStatus" column="risk_status"/>
        <result property="riskReason" column="risk_reason"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.unicorn.gameshop.order.model.Order">
        INSERT INTO orders (id, order_no, buyer_id, product_id, game_id, player_uid, quantity, amount_minor,
                            currency, country, access_token_hash, order_status, payment_status, fulfillment_status,
                            risk_status, risk_reason,
                            expires_at, created_at, updated_at)
        VALUES (#{id}, #{orderNo}, #{buyerId}, #{productId}, #{gameId}, #{playerUid}, #{quantity}, #{amountMinor},
                #{currency}, #{country}, #{accessTokenHash}, #{orderStatus}, #{paymentStatus}, #{fulfillmentStatus},
                #{riskStatus}, #{riskReason},
                #{expiresAt}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findById" resultMap="orderMap">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <select id="findByOrderNo" resultMap="orderMap">
        SELECT * FROM orders WHERE order_no = #{orderNo}
    </select>

    <update id="updatePayment">
        UPDATE orders
        SET order_status = #{orderStatus}, payment_status = #{paymentStatus},
            fulfillment_status = #{fulfillmentStatus}, updated_at = #{updatedAt}
        WHERE id = #{orderId}
    </update>

    <update id="updateFulfillment">
        UPDATE orders
        SET order_status = #{orderStatus}, fulfillment_status = #{fulfillmentStatus}, updated_at = #{updatedAt}
        WHERE id = #{orderId}
    </update>

    <update id="expirePending">
        UPDATE orders
        SET order_status = 'EXPIRED', updated_at = #{updatedAt}
        WHERE order_status = 'CREATED' AND expires_at &lt; #{now}
    </update>

    <select id="countRecentByPlayerAndProduct" resultType="int">
        SELECT COUNT(1) FROM orders
        WHERE player_uid = #{playerUid} AND product_id = #{productId} AND created_at &gt;= #{createdAfter}
          AND order_status NOT IN ('CANCELED', 'EXPIRED')
    </select>

    <update id="updateRisk">
        UPDATE orders
        SET risk_status = #{riskStatus}, risk_reason = #{riskReason},
            fulfillment_status = #{fulfillmentStatus}, updated_at = #{updatedAt}
        WHERE id = #{orderId}
    </update>
</mapper>
