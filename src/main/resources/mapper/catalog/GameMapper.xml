<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unicorn.gameshop.catalog.mapper.GameMapper">
    <resultMap id="gameMap" type="com.unicorn.gameshop.catalog.model.Game">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="publisher" column="publisher"/>
        <result property="supportedPlatforms" column="supported_platforms"/>
        <result property="deliveryType" column="delivery_type"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.unicorn.gameshop.catalog.model.Game">
        INSERT INTO games (id, code, name, publisher, supported_platforms, delivery_type, status, created_at, updated_at)
        VALUES (#{id}, #{code}, #{name}, #{publisher}, #{supportedPlatforms}, #{deliveryType}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.unicorn.gameshop.catalog.model.Game">
        UPDATE games
        SET code = #{code}, name = #{name}, publisher = #{publisher}, supported_platforms = #{supportedPlatforms},
            delivery_type = #{deliveryType}, status = #{status}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <select id="findById" resultMap="gameMap">SELECT * FROM games WHERE id = #{id}</select>
    <select id="list" resultMap="gameMap">
        SELECT * FROM games
        <where><if test="status != null and status != ''">status = #{status}</if></where>
        ORDER BY created_at DESC
    </select>
</mapper>
